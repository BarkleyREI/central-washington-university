<figure class="video">

	<img src="{{ poster.src }}" alt="{{ poster.alt }}" class="video__poster" width="{{ poster.width }}" height="{{ poster.height }}" />
	
	{{render '@button--xlarge' play-button }}
	
</figure>